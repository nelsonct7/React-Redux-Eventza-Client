import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  maindiv:{
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 5,
    margin:50
  }
}));